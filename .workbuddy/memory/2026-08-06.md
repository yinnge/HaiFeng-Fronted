# 2026-08-06 工作日志

## user 端全站暖橙背景统一
- 需求：把「直通考院」区块的泛橙背景（`#fff7ed` 渐变 + 橙/琥珀径向光晕）推广到 user 端所有页面。范围经确认：**仅 apps/user，不含 admin**（admin 有独立企业级设计体系）。
- 根因调查：全局样式 `assets/styles/index.css` 的 body 没设背景；DefaultLayout 是无背景壳；约 60 个页面的背景是各自根 div 写死的 `bg-white / from-slate-50 to-white / bg-slate-50 / bg-gray-50`。
- 实现（2 处，覆盖全站，不逐个改 60 文件）：
  1. `assets/styles/index.css`：给 `html` 设统一暖橙画布——`background-image: radial-gradient(橙0.1) + radial-gradient(琥珀0.08) + linear-gradient(#fff→#fff7ed→#fff7ed)`，`background-attachment: fixed`；并加规则 `.app-shell main > *, #app > * { background-color: transparent !important; background-image: none !important }` 把页面根不透明背景透掉（只作用根节点，内部白卡不受影响）。
  2. `layouts/DefaultLayout.vue`：根 div 加 `app-shell` 类作为选择器锚点。
- 效果：60 页 + 登录/注册/404 一次性变暖橙，内部卡片仍白色；Vite HMR 即时生效。色值严格取自 `#exams` 区块。
- 注意：登录/注册/404 走 `layout: 'blank'`（App.vue `<router-view v-else>`），由 `#app > *` 覆盖；若某 blank 页自定义全屏渐变被透掉属预期（统一风格）。

## 暖橙背景微调（同日，去单调）
- 用户反馈首版全站背景"太单调、只有暖橙色"，想要「直通考院」那种"白底透橘 + 白色涂抹 + 点缀光晕"的层次感，且不能太绚丽。
- 修正（仍只改 `index.css` 的 `html` 根画布）：
  - 竖向渐变由 `#fff→#fff7ed55%→#fff7ed100%`（下半截纯橘）改为 `#fff→#fffaf3 38%→#fff7ed 55%→#fff7ed 64%→#fff`（首尾透白，橘只居中段）。
  - 新增三层柔白涂抹（顶部/中部/底部 `rgba(255,255,255,…)` 径向渐变）制造"白底透橘"通透层次。
  - 点缀光晕由 2 道增为 3 道（左上橙、右上琥珀、下中橙），透明度压低不喧宾夺主。
- 该背景为全站统一（`background-attachment: fixed` 钉视口），首页即预览页。Vite HMR 即生效。

## 研究生专栏开发（user 端静态页）
- 需求：导航栏「研究生专栏」下拉开发。① 研究生资料 → 点「研究生专栏」或「研究生资料」都进同一页（参考 hfwep/postgraduate-overview.html，样式换我们的暖橙体系）；② 申博指南 → 静态页（参考 phd-application.html）。页面内嵌：研一/研二/研三/申博 4 个阶段卡片，英语四六级（点击报"该功能正在开发中"）、学术资料（跳 /resource）、高层次就业方向（跳 /employment/civil）。
- 新增 5 个文件 `apps/user/src/views/postgraduate/`：index.vue（研究生专栏主页，4 阶段卡 + 3 核心模块卡）、first-year.vue、second-year.vue、third-year.vue、phd.vue（申博，6 步流程 + 3 组策略卡 + FAQ 手风琴）。全部静态数据驱动（script 数组 + v-for），风格统一：白卡 + 橙渐变顶边（border-image #f97316→#fb923c）+ 暖橙底 hero + 药丸按钮 + emoji 图标（项目未引入 Font Awesome，用 emoji 替代 fas 图标）。
- 路由（router/index.ts）：`/postgraduate`（主页）、`/postgraduate/first-year`、`/postgraduate/second-year`、`/postgraduate/third-year`、`/postgraduate/phd`；均无 requiresAuth（静态内容）。跳转用 `router.push` 相对路径（不写死 IP，部署后自动跟随域名）。
- 导航（config/navigation.ts）：postgrad 项加 `route: '/postgraduate'`（点顶部也进主页），subItems 补 route：研究生资料 → `/postgraduate`、申博指南 → `/postgraduate/phd`。MobileNavDrawer 复用同一配置无需改。顶部有 route 的父项点击 = 直接 router.push（handleNavClick 逻辑现支持）。
- 验证：user vue-tsc 0 新增错误（仅剩 PlanDetail.vue 3 个存量，用户明确不处理）；Vite 编译 5 页全 200。

## 院校适应指南 user 端 JSONB 英文→中文
- 问题：`apps/user/src/views/university/Guide.vue` 用 `v-for="(value, key) in categoryData"` 直接把后端 JSONB 的**英文顶层字段名**（campusFacilities / dormitoryServices 等）当标题渲染，用户看到英文。
- 修复：把 admin `apps/admin/src/views/university/guide/index.vue` 的 `JSONB_FIELDS`（英文 key → 中文 label/subFields，14 组）原样搬进 Guide.vue `<script>`，加 `fieldLabelMap`/`fieldSubMap` 两个查表 + `getFieldLabel(key)`/`getSubLabel(fieldKey, subKey)` 辅助函数（参数类型 `string | number`，内部 `String()` 归一，找不到回退原值兼容历史数据）。模板 `{{ key }}`→`{{ getFieldLabel(key) }}`、`{{ k }}`→`{{ getSubLabel(key, k) }}`。
- 验证：user vue-tsc 中 Guide.vue 0 错误；dev server 3001 编译 200。仅剩 PlanDetail.vue 3 个存量错误（用户不处理）。
- 注意：admin 端存储的子字段键本就是中文（buildEmptyJsonb 用中文 subFields 作键），所以 subFields 这层正常应是中文；顶层 key 永远是英文，靠本映射转中文。

## admin 院系管理 dept/index.vue：修复 jsonb 详情保存 + 补缺失字段 + 新增也带详情 tab
- 三件事全部落地，admin `vue-tsc --noEmit` 0 错误：
  1. **保存丢失根因修复**：后端 `DepartmentDetailVO` 是**扁平字段**（无 `report` 嵌套），旧代码 edit 回填误读 `d.report.xxx` 导致 `reportData` 永不回填；提交时把空串传回，后端 `if(dto.getXxx()!=null)` 用空串覆盖原数据 → 改完点保存之前数据被清空。改为按扁平字段读 `d.subtitle/d.career/d.subjectsDetail…` 回填（prospects/disclaimer 用 `?? ''` 显式兜底，避免 `undefined` 不赋得进 reactive）。
  2. **补缺失 jsonb 字段**：新增 `career`(职业路径→`CareerEditor.vue`，含 pathTitle/pathDesc + 嵌套 stages)、`subjectsDetail`(专业详情对象→`SubjectsDetailEditor.vue`)、`salary`(专业薪资)、`majorCompose`(学科组成)。新增共用组件 `ReportForm.vue`（聚合所有 jsonb 区块，props `reportData` 与父共享引用直接读写字段），并由 `DepartmentAddDTO`/`DepartmentUpdateDTO` 补全部 report 字段。
  3. **新增也带详情 tab**：add 与 edit 模式都用 `el-tabs`，`基本信息` + `院系详情`(嵌 `<ReportForm :report-data="reportData" />`)；`handleSubmit` add 分支补传 reportData 全部字段；`resetReportData()` 在 openDialog add 分支清空。
- 注意：detail 只读视图相应把 `detailData.report.xxx` 全改为 `detailData.xxx`，并对可选嵌套加 `?.`（overview/prospects/trends/postgraduate）防 undefined。

## admin 侧边栏按权限隐藏无权限模块（permission 需求）
- 需求：管理员无某模块权限时，左侧导航栏不再展示该模块（原先全部展示、点了才报 403/404）。
- 现状：数据链路本来就通——登录 `useAuth.handleLoginSuccess` 已 `fetchProfile()`；后端 `ProfileServiceImpl.getProfile` 用 `SysAdminMapper.selectModuleCodesByAdminId`（sys_admin→sys_role_module→sys_module 三表联查）返回 `moduleCodes`；路由 meta 都带 `moduleCode`；Sidebar `buildMenu` 也有过滤逻辑。**真正的 bug 在两处**：
  1. `Sidebar.vue buildMenu`：父级路由自身无 moduleCode（纯分组，如「就业管理」），子项全被权限过滤后仍 `return item`（children 为空）→ 父级渲染成可点击的橙色空叶子，点击跳 redirect → 403。修法：`visibleChildren.length === 0` 时 `return null`，整个父级隐藏（含三级嵌套自动递归剪枝）。
  2. `router/index.ts` 守卫：原来只有 `to.meta?.moduleCode` 存在才 `fetchProfile`，刷新后落在无 moduleCode 页面（/dashboard）时 profile 为 null，Sidebar 过滤条件 `if (userStore.profile && ...)` 被跳过 → 全部展示。修法：token 存在时先无条件 `await fetchProfile()`（失败 catch 不阻塞），再做 moduleCode 鉴权。
- 结果：无权限的叶子隐藏、无可见子项的父级整组隐藏、无 moduleCode 的 dashboard 恒展示；typecheck 0 错误。
- 经验：凡是"侧边栏/菜单按 meta.moduleCode + profile.moduleCodes 过滤"的页面，改布局时记得同时验证 ① 父级无子项时的剪枝 ② 守卫是否保证 profile 先加载。

## admin 实验室 laboratory/index.vue：实验室类型支持自定义值
- 需求：新增/修改弹窗里「实验室类型」原本是写死 5 个固定值的 `el-select`，要保留固定值同时支持手填自定义值。
- 修法：表单里 labType 的 `el-select` 加 `filterable allow-create default-first-option`（与同文件 researchFields/majorEquipment 一致套路），placeholder 改为"请选择或输入自定义类型"。`labTypeOptions` 数组（5 个固定值：国家重点/教育部重点/省级重点/国家工程实验室/国家工程研究中心）原样保留作为预设。搜索区筛选下拉不受影响（仍纯固定值）。
- 验证：admin vue-tsc --noEmit 0 错误。

## admin 竞赛专业关联 competition-major/index.vue：新增表单名称联想
- 需求：新增关联时输入「竞赛名称/专业名称」希望下拉提示已存在的名称，方便关联复用。
- 实现：用 `el-autocomplete` 替换原 `el-input`（两个字段都换）。开弹窗时 `fetchNameSuggestions()` 调 `getCompetitionMajorPage` 分页（size=100，循环累加直到不足一页或 50 页上限）拉全量记录，去重得到 `competitionNameSuggestions`/`majorNameSuggestions` 两个列表；`:fetch-suggestions` 用 `queryNameSuggestions()` 按输入做不区分大小写 `includes` 过滤后 `cb([{value}])`。无专用"去重名称"接口，故走现有 list 接口分页聚合并去重。
- 注意：后端 `BasePageQueryDTO.size` 有 `@Max(100)`，所以**不能用 size:1000 一次拉全**，必须分页循环（这次踩到约束，已规避）。联想失败静默降级为普通输入。
- 验证：admin vue-tsc --noEmit 0 错误。

## admin competition-major 合并拉取后报错修复（merge 冲突）
- 现象：用户 merge 仓库并 pull 后 `competition-major/index.vue` 报 2 个 TS 错误：
  - `(462,30): Property 'competitionName' does not exist on type '{ competitionId: number; majorId: number; }'`
  - `(484,30): Property 'majorName' does not exist`
- 根因：upstream 把新增表单从「按名称文字输入」重写成「按 ID 远程搜索的 el-select」（`addForm` 改为 `competitionId`/`majorId` 两个 number 字段，新增 `getCompetitionPage`/`getMajorPage` 远程搜索 + `competitionOptions`/`majorOptions`）。但 git merge 把我之前加的 `el-autocomplete`（竞赛名称/专业名称）**两块残留**也并了进来，且结构错位（第一个 `el-form-item` 没闭合就嵌套了新 `el-form-item`），引用的 `addForm.competitionName/majorName` 已不存在 → 编译报错。
- 修法：删掉我那两块 orphaned autocomplete（模板 4 行 ×2 + 脚本 `competitionNameSuggestions`/`majorNameSuggestions`/`fetchNameSuggestions`/`queryNameSuggestions` 整段 + `openAddDialog` 里的 `fetchNameSuggestions()` 调用），保留 upstream 的 ID 远程搜索 select。**upstream 方案本身就满足"下拉提示已有数据"的需求**（远程模糊搜竞赛/专业名→选 ID），故无需保留我那版。
- 验证：admin vue-tsc --noEmit 0 错误（首次全量退出码误报 1，重跑为 0，确认无错）。
- 经验：merge/rebase 后若某文件既有 upstream 新结构又有本地旧片段残留，优先比对"字段定义 vs 模板引用"定位孤儿引用；本例 `addForm` 类型变了但模板仍引用旧字段名，精准暴露冲突点。

## admin 资源管理列表删除 Excel 导入
- 任务：`apps/admin/src/views/resource/list/index.vue` 移除「Excel导入」按钮（无对应后端接口）。
- 删除内容：① script 里 `importResource` 引入 + `importDialogVisible`/`importFile`/`importLoading` 三个 ref + `handleImportFileChange`/`handleImportSubmit` 两个函数；② 模板「Excel导入」按钮；③ 整个导入 `<el-dialog>`（含 el-upload）；④ 仅用于导入的 `.loading-spinner` / `@keyframes spin` CSS。
- 连带清理：`apps/admin/src/api/resource/index.ts` 里 `importResource` 已无任何调用方（死代码），一并删除。
- 验证：admin `vue-tsc --noEmit` 0 错误。

## admin 专业组管理删除「明细」按钮
- 任务：`apps/admin/src/views/algorithm/admission/group/index.vue` 移除表格操作列的「明细」按钮（原跳 `/algorithm/admission/major-score` 专业明细列表）。
- 删除：① 模板操作列 `<button class="action-pill action-detail" @click="goToMajorScore(row.id)">明细</button>`；② `goToMajorScore` 函数；③ 因仅该函数用 `router`，连带移除 `useRouter` 引入与 `const router = useRouter()`；④ 无用 CSS 类 `.action-detail`。
- 验证：admin `vue-tsc --noEmit` 中 admission/group 0 错误；无 goToMajorScore/useRouter/action-detail 残留引用（删除确认文案中的"专业明细"是语义说明，保留）。
## 高考算法模块：全局逻辑删除干扰修复（2026-08-06）

### 根因
后端 `application-dev/prod.yml` 配置了全局 `logic-delete-field: isDeleted`，导致所有有 `isDeleted` 字段的实体（不管有无 `@TableLogic`），MP 内置方法（`selectOne`/`updateById`）都会自动：
- SELECT 追加 `AND is_deleted=false`
- UPDATE 追加 `AND is_deleted=false` 到 WHERE，且从 SET 排除 `is_deleted`

### 修复清单（共 4 后端文件 + 1 前端文件）

**问题1：专业组「删除」不生效**
- 文件：`AdmissionGroupServiceImpl.java` → `delete()` 方法
- 改前：`entity.setIsDeleted(true) + updateById(entity)` → MP 排除 is_deleted SET → 无效
- 改后：`admissionGroupMapper.updateIsDeletedById(id, true)`（自定义 @Update）

**问题2+3+4：专业组列表默认筛选 + 文案**
- 文件：`group/index.vue`
- `isDeleted` 默认 `false` → `undefined`（展示全部），reset 同步
- 按钮 `批量软删除` → `批量禁用`，确认文案同步

**问题5：专业明细 selectByIdCustom 查不到禁用数据**
- 文件：`AdmissionMajorScoreMapper.java` → `selectByIdCustom`
- 改前：`default` 方法用 `selectOne(LambdaQueryWrapper)` → MP 追加 is_deleted=false
- 改后：自定义 `@Select("SELECT * FROM t_admission_major_score WHERE id = #{id}")`
- 同时 `AdmissionMajorScoreServiceImpl.update()` 移除 `|| existing.getIsDeleted()` 判断

**额外修复：AdmissionGroupMapper.findByIdIgnoreLogicDelete**
- 文件：`AdmissionGroupMapper.java`
- 同 selectByIdCustom 问题，改为自定义 `@Select`
- 同时 `AdmissionGroupServiceImpl.update()` 移除 `|| existing.getIsDeleted()` 判断

**问题6：一分一段次修改禁用记录静默失败**
- 文件：`ScoreRankMapper.java` + `ScoreRankServiceImpl.java`
- `ScoreRank` 有 `@TableLogic`，`updateById` 追加 `is_deleted=false` → 禁用记录 0 行更新不报错
- 新增 `updateByIdCustom` 自定义 `@Update` 绕过逻辑删除 WHERE

**问题7：专业组「修改」成功但数据没变**
- 文件：`AdmissionGroupMapper.java` + `AdmissionGroupServiceImpl.java`
- 根因：`update()` 方法中 `updateById(existing)` 与 `@Version` 乐观锁 + 全局逻辑删除叠加，可能返回 0 行但未检查返回值 → 静默成功
- 新增 `updateByIdCustom` 自定义 `@Update`（绕过 @Version 和逻辑删除），Service 层检查返回值，0 行抛 BusinessException

**附带修复：前端 group/index.vue 枚举值修正**
- `requirementTypeOptions`：`['2年','3年','必备','必备','必备']` → `['2选1','3选1','必选1','必选2','必选3']`
- `batchOptions`：`['本科','提前','专科']` → `['本科批','提前批','专科批']`

### 经验
- 判断是否为全局逻辑删除的"排除 SET"问题：看 `updateById` 后 `is_deleted` 是否没变
- 判断是否为全局逻辑删除的"追加 WHERE"问题：看 `selectOne` 是否查不到已删除/禁用记录
- 标准修复：自定义 `@Select`/`@Update` 注解（不受 MP 拦截器影响）

**问题8：专业明细「修改」禁用记录静默失败 + 删除不生效**
- 文件：`AdmissionMajorScoreMapper.java` + `AdmissionMajorScoreServiceImpl.java`
- `update()` 第 133 行 `updateById(existing)` → 禁用记录追加 `WHERE is_deleted=false` → 0 行静默成功
- `delete()` 第 156-157 行 `entity.setIsDeleted(true) + updateById(entity)` → MP 排除 is_deleted SET → 标识写不进去
- 新增 `updateByIdCustom` 自定义 `@Update`（绕过逻辑删除），update() 检查返回值；delete() 改用 `updateIsDeletedById`

**问题9：城市「修改」禁用记录静默失败**
- 文件：`CityMapper.java` + `CityServiceImpl.java`
- `update()` 用 `findByIdIgnoreLogicDelete` 查，但 `updateById` 追加 `WHERE is_deleted=false` → 同问题7
- 新增 `updateByIdCustom` 自定义 `@Update`，Service 检查返回值

**问题10：资源「删除」不生效**
- 文件：`ResourceServiceImpl.java`
- `delete()` 使用 `entity.setIsDeleted(true) + updateById(entity)` → MP 排除 isDeleted SET
- 改用已有的 `updateIsDeletedById(id, true, now)`（该 Mapper 本就已有此自定义方法）

**问题11：修改禁用城市时详情报 404**
- 文件：`CityServiceImpl.java` → `updateDetail()`
- 前端修改城市同时调 `updateCity` + `updateCityDetail`，禁用城市的详情也被禁用 → `findByCityId`（MP 内置方法）查不到 → 404
- 改为当详情不存在/已禁用时 `log.warn + return`（跳过详情更新，不报错）
