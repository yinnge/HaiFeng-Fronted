# 2026-08-04 工作日志

## 用户端就业信息专栏前后端字段一致性检查（分析，未改动代码，等用户确认）
- 检查范围：apps/user employment 10 个岗位模块（civil/institution/military/selection/teacher/healthcare/finance/community/welfare/grassroots）List+Detail，以及 content（examGuide/notice）4 个组件。后端对比 haifeng-app 的 Controller/DTO/VO/ServiceImpl + V20-V23 迁移。
- **已对齐**：10 个岗位模块 URL 路径、分页参数（page/size 默认10、page-sizes [10,20,30,50,100]，满足 common BasePageQueryDTO size∈[10,100]）、筛选字段、详情字段全部与后端一致；详情接口 @RequireLogin 前端均有登录拦截。
- **发现的问题（待确认后修）**：
  1. **content 接口全挂**：前端调 `/content/notice/list`、`/content/notice/{id}`、`/content/exam-guide/list`、`/content/exam-guide/{id}`；后端只有 `/list-by-type?noticeCategory=&noticeType=`（返回 List 无分页）。NoticeDialog/ExamGuideDialog 打开即 404。前端类型还有后端 VO 没有的 viewCount/likeCount。
  2. **community 列表缺 positionStatus**：后端 CommunityPositionListVO 无此字段（detail 有），前端 List.vue 状态徽标永远空白。筛选传 positionStatus 正常（DTO/SQL 都有）。
  3. **grassroots 列表缺 positionStatus/regStartDate/regEndDate**：后端 GrassrootsProjectPositionListVO 无这 3 个字段（detail 有），前端状态徽标+报名时间空白。
  4. **welfare 列表缺 positionStatus**：后端 PublicWelfarePositionListVO 无，前端状态徽标空白。
  5. **grassroots 筛选多出 targetGroup/maxServiceYears**：后端 SearchDTO 无这 2 个字段（welfare 有），选了不生效（Spring 忽略未知参数不报错）。
- **轻微**：healthcare/institution 前端 ListVO 类型缺后端返回的 2 个字段（页面未用，无功能影响）。
- **提示**：civil 筛选"报名状态"缺"即将开始"选项（DB CHECK 有 3 态）；详情 contactPhone 后端 desensitizePhone 脱敏（community/welfare/grassroots）。

## 用户端就业专栏问题修复（方案 A，用户确认后实施，未 git 提交）
### 后端 haifeng-app（需重启生效）
- **content 补分页/详情接口**（问题1）：
  - 新增 `dto/employment/contentManagement/notice/NoticeQueryDTO.java`、`examGuide/ExamGuideQueryDTO.java`（继承 BasePageQueryDTO；keyword 由前端映射为 title/summary/source 或 title/subtitle/authorTitle/authorName，SQL 用 OR like 组合）
  - `NoticeService/NoticeServiceImpl`、`ExamGuideService/ExamGuideServiceImpl` 加 `page(dto)`（分类/类型/省/市/年份 eq + 关键词 OR like + sort_order DESC NULLS LAST, created_at DESC NULLS LAST 分页）、`detail(id)`（不存在抛 NOT_FOUND）
  - `NoticeController`、`ExamGuideController` 补 `GET /list`（分页）+ `GET /{id}/detail`（@RequireLogin），保留原 list-by-type
  - `NoticeDetailVO` +viewCount；`ExamGuideDetailVO` +viewCount/likeCount（t_notice 无 likeCount 列，t_exam_guide 有）
- **列表补字段**（问题2/3/4）：`CommunityPositionListVO` +positionStatus；`GrassrootsProjectPositionListVO` +positionStatus/regStartDate/regEndDate；`PublicWelfarePositionListVO` +positionStatus；对应 3 个 ServiceImpl.page() builder 同步补。前端 List 状态徽标/报名时间恢复显示。
### 前端 apps/user
- 问题5：`views/employment/grassroots/List.vue` 删「面向群体/服务年限」筛选（ref/选项/buildParams/onReset/isFilterActive/模板）；`types/employment/grassroots/index.ts` GrassrootsQueryDTO 删 targetGroup/maxServiceYears。welfare 模块的这两个字段保留（后端有）。
- content 前端零改动（API 路径 /list、/{id} 后端已补齐）。
### 验证
- 后端 `mvnw.cmd -pl haifeng-app -am compile` 通过；前端 `node node_modules/vue-tsc/bin/vue-tsc.js --noEmit`（user 包）通过。
- 本机环境坑：git bash 里 `./mvnw` 缺 cygpath 跑不了、`mvn` 主类找不到 → 用 PowerShell 跑 `.\mvnw.cmd`；`pnpm` 全局路径损坏 → 直接 `node node_modules/vue-tsc/bin/vue-tsc.js --noEmit -p tsconfig.json`。

## 就业信息专栏搜索栏优化（部分固定值修改 + 部分动态化）

### 后端（haifeng-app，需重启生效）
新增4个 `/filters` 公开接口（无需登录），每个返回对应字段的 SELECT DISTINCT 去重列表：

| 模块 | 接口 | 返回字段 |
|------|------|---------|
| 公务员 | `GET /api/v1/app/employment/civil-service/position/filters` | examCategory |
| 事业编 | `GET /api/v1/app/employment/civil-service/institution/filters` | examCategory, positionType, specialPosition |
| 医疗卫生 | `GET /api/v1/app/employment/healthcare/filters` | department |
| 金融银行 | `GET /api/v1/app/employment/finance/filters` | positionCategory |

涉及文件（共16个）：
- haifeng-common 4个 Mapper：新增 @Select DISTINCT 方法
- haifeng-app 4个 Service 接口 + 4个 ServiceImpl：新增 getFilters()
- haifeng-app 4个 Controller：新增 GET /filters

### 前端用户端（apps/user，Vite HMR 即时生效）
| 页面 | 改动 | 需求 |
|------|------|------|
| civil/List.vue | examCategory 动态获取 + regStatus 加"即将开始" | 需求1+2 |
| institution/List.vue | examCategory/positionType/specialPosition 动态获取 | 需求3/5 |
| military/List.vue | 岗位类型改为 `['管理岗','专业技术岗']`（删技能岗，命名统一） | 需求4 |
| grassroots/List.vue | 学历要求对齐 DB：`['大专','本科','硕士','大专及以上','本科及以上']` | 需求6 |
| community/List.vue | 岗位类型9个正确值 + 学历要求 `['不限','高中','大专','本科','硕士']` | 需求7 |
| welfare/List.vue | 学历要求 `['不限','初中','高中','大专','本科']` | 需求8 |
| healthcare/List.vue | 科室下拉改为从 /filters 动态获取 | 需求9 |
| finance/List.vue | 机构类型缩减为8个（对齐DB）+ 岗位类别动态获取 | 需求10 |

### 前端管理端（apps/admin）
| CivilSearch.vue | regStatusOptions 加"即将开始" | 需求2 |
| MilitaryFormModal.vue | positionTypeOptions 删"专业技能岗" | 需求4 |
| MilitarySearch.vue | positionTypeOptions 删"专业技能岗" | 需求4 |

### 验证
- 用户端 typecheck 通过（无错误）
- 管理端 typecheck 通过（报告的全是存量错误，非本次改动引入）
