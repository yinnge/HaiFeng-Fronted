# 2026-07-27 工作日志

## 公告管理页面（admin/home/announcement）橘红主题重构

仿照 `apps/admin/src/views/user/order/` 的组件化结构与橙色主题规范（AGENTS.md），重构了公告管理页面。

### 改动内容

**新建组件（`apps/admin/src/views/home/announcement/components/`）：**
1. `AnnouncementSearch.vue` — 搜索筛选栏：橙色顶底边框卡片 + 药丸标题"筛选条件" + 左对齐筛选字段 + 右对齐查询/重置按钮（橙色渐变药丸 + 描边圆角）
2. `AnnouncementTable.vue` — 表格卡片：橙色渐变表头 + 斑马纹 + 行 hover 浅橙渐变 + 标签/状态药丸 + 操作按钮渐变药丸（详情橙/修改蓝/启用绿/禁用黄/删除红）+ 自定义分页（当前页橙色渐变）
3. `AnnouncementDetailModal.vue` — 详情弹窗：圆角 12px + 标题栏橙色分隔线 + el-descriptions 标签列浅橙背景 + HTML 内容展示区
4. `AnnouncementFormModal.vue` — 新增/修改弹窗：圆角 + 橙色分隔线 + 橙色主题表单输入 + 提交（橙色渐变药丸）/取消（描边圆角）按钮

**重写 `index.vue`：**
- 暖色渐变页面背景 + 枫叶水印（右上+左下，opacity 0.05）
- 页面标题"公告管理" + 副标题
- 操作栏：新增公告（橙色渐变药丸带 + 图标）+ 刷新（描边圆角）
- 拆分原单文件 dialog 为 DetailModal + FormModal 两个独立组件
- 保留全部原有功能：列表查询、分页、详情、新增、修改、启用/禁用切换、删除

### 设计规范遵循
- 主色 #F97316，渐变端 #FB923C
- 卡片：白底 + 12px 圆角 + 顶 3px #F97316 / 底 3px #FB923C
- 按钮：border-radius 20px 药丸形
- 表头：橙色渐变 + 白色粗体
- 未使用任何 Element Plus 默认灰色按钮 / 默认 el-card / 默认表头样式

### 验证
- `vue-tsc --noEmit` 类型检查通过（announcement 相关文件无错误）
- dev server localhost:3000 运行中

---

## 规划师管理页面（admin/home/planner）橘红主题重构

仿照 `user/order` 与 `home/announcement` 的组件化结构，重构了规划师管理页面。

### 新建组件（`apps/admin/src/views/home/planner/components/`）：
1. `PlannerSearch.vue` — 搜索栏：橙色卡片 + 药丸标题 + 姓名/状态筛选
2. `PlannerTable.vue` — 表格：橙色渐变表头 + 地区蓝色药丸 + 状态药丸 + 5色操作按钮 + 橙色分页
3. `PlannerDetailModal.vue` — 详情弹窗：头像展示 + 成就橙/擅长领域绿双色药丸标签 + 橙色分隔线
4. `PlannerFormModal.vue` — 新增/修改弹窗：双列布局 + 成就/擅长领域动态数组（橙色虚线添加按钮 + 红色删除按钮）

### 重写 `index.vue`：
- 暖色渐变背景 + 枫叶水印 + 标题副标题 + 操作栏（新增/刷新）
- 保留全部原有功能：CRUD + 状态切换 + 成就/擅长领域动态增删

### 验证
- `vue-tsc --noEmit` 类型检查通过（planner 相关文件无错误）

---

## 院校信息管理页面（admin/university/info）橘红主题重构

仿照 order/announcement/planner 的组件化结构与橙色主题规范，重构了院校信息管理页面。

### 新建组件（`apps/admin/src/views/university/info/components/`）：
1. `UniversitySearch.vue` — 搜索栏：4 字段筛选（名称/省份/类别 11 选项/状态）
2. `UniversityTable.vue` — 操作栏 + 表格卡片：新增/导入主表/导入详情/批量永久删除（红色药丸）+ 刷新，多选 + 办学层次绿色药丸 + 性质蓝色药丸 + 状态药丸
3. `UniversityDetailModal.vue` — 详情弹窗：双 Tab（基础信息 + 详细信息含轮播图预览 + 5 项排名）+ 推荐率高亮
4. `UniversityFormModal.vue` — 新增/修改弹窗：双 Tab（基础信息 14 字段 + 详细信息含排名段）+ 双表单数据（formData + detailFormData）

### 重写 `index.vue`：
- 暖色渐变背景 + 枫叶水印 + 标题副标题
- UniversityTable 整合操作栏（导入/批量操作按钮）
- FormModal 通过 initialForm + initialDetail 双 prop 传入初始数据，submit 事件传出双数据
- 保留全部功能：CRUD + 状态切换 + 多选批量删除 + Excel 导入主表/详情 + 软删除/硬删除

### 验证
- `vue-tsc --noEmit` 类型检查通过（university/info 相关文件无错误）

---

## 批量重构：12 页面橘红主题 + 类型错误修复（22:49-23:XX）

统一将下面 12 个页面改为橘红主题（品牌色 #F97316/#FB923C），单文件样式覆盖方式（不拆组件，效率优先），同步修复所有类型错误。

### 处理结果

| # | 页面路径 | 原错误数 | 现错误数 | 修复内容 |
|---|---------|---------|---------|---------|
| 1 | `university/guide` | 1 | 0 | TS2345 批量删除 + 主题 |
| 2 | `university/dept` | 2 | 0 | TS2345 + TS2322 + 主题 |
| 3 | `university/laboratory` | 2 | 0 | TS2345 + TS2322 + 主题 |
| 4 | `university/subject-evaluation` | 2 | 0 | TS2345(×2) + 主题 |
| 5 | `major/list` | 2 | 0 | TS2322(×2) + 主题 |
| 6 | `major/postgrad` | 2 | 0 | TS2322(×2) + 主题 |
| 7 | `major/postgrad-univ` | **8** | 0 | status DTO 缺失 + TS2322(×2) + 主题 |
| 8 | `major/postgrad-direction` | 5 | 0 | TS2322(×4) + TS2345 + 主题 |
| 9 | `city/list` | 3 | 0 | TS2322(×2) + TS2345 + 主题（含 _fixed.vue） |
| 10 | `industry/list` | 1 | 0 | TS2345 + 主题 |
| 11 | `resource/list` | 1 | 0 | TS2345 + 主题 |
| 12 | `certificate/certificate` | 2 | 0 | TS2345(×2) + 主题 |

**总计：12 页面，31→0 错误，全部统一橘红主题**

### 统一规范
每个页面均应用：暖色渐变背景 + 枫叶水印 + 标题副标题 + search-card（橙色边框 + 药丸标题）+ action-bar（渐变/描边/红色药丸按钮）+ table-card（橙色渐变表头 + 药丸标签 + 5 色操作按钮）+ custom-pagination（橙色当前页）+ uni-dialog（圆角 + 橙色分隔线 + 浅橙标签列）

---

## Profile + Navbar 优化（23:XX）

### 1. Profile 页面（`/profile`）
仿 user 端的金色橙色渐变风格重构，重写全部 5 个组件：

- `index.vue`：暖色渐变背景 + 枫叶水印 + 页面标题副标题 + 左右布局容器
- `AvatarCard.vue`：仿 user 端 `ProfileHeader.vue` 风格 — 88px 环形渐变头像（4px padding + 2px 内白边 + 3px 内白边）、背景装饰圆、角色徽章（橙色药丸）、TOTP 状态指示（绿点/灰点 + 文字）、红色渐变退出登录按钮
- `ProfileForm.vue`：信息区用 `<el-row :gutter="24">` 双列布局（24px 间距）、3 个分区（基本信息 / 修改密码 / TOTP 认证）独立卡片化设计、密码和 TOTP 区域使用 action-card（图标 + 标题 + 描述 + 按钮）布局、按钮统一样式
- `PasswordModal.vue`：460px 宽、自定义 header（蓝色密码图标 + 标题副标题）、橙色边框圆角
- `TotpModal.vue`：460px 宽、提示框橙色/红色、QR 码区域橙色虚线边框、密钥框橙色渐变背景、按钮统一渐变药丸

### 2. Navbar 改造（admin/layout/components/Navbar.vue）
- **删除** "进入后台管理" 按钮
- **重写**：自建用户头像下拉（抛弃 el-dropdown），仿 user 端 `UserAvatar.vue` 风格 — 触发器金色描边 + 橙色 hover、头像环形橙色边框 + 阴影、下拉菜单 240px 宽含用户信息卡 + 分割线 + 菜单项、菜单项 hover 橙色、退出项红色

### 验证
- `vue-tsc --noEmit` 类型检查通过（profile + Navbar 相关文件无错误）
