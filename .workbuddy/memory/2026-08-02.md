# 2026-08-02 工作日志

## 事件：git pull --rebase 覆盖丢失未提交修改 → 从 stash 恢复城市模块
- 用户反馈：city/list 编辑弹窗又变成"textarea 手填 JSON"旧样式，质疑"不是修复过了吗"。
- 排查：① 当前文件 850px/全 span=12/无 section-block/JsonbArrayEditor → 是旧版本；② git log 确认**所有提交历史都不含结构化表单**（我的修复从未 commit）；③ reflog 发现今天 14:36 `git pull --rebase origin main: Fast-forward` → **未提交的工作区改动被 rebase 覆盖丢弃**；④ `git stash list` 找到 stash@{2}（WIP on yinnge: 46d7766）含完整修复版 index.vue（1801 行：1000px + 45 处 span=8 + 61 处 controls-position + 结构化 JSONB 表单）+ JsonbArrayEditor.vue/JsonbObjectEditor.vue。
- 恢复（git show "stash@{2}:path" > path）：`apps/admin/src/views/city/list/index.vue`、`apps/admin/src/components/JsonbArrayEditor.vue`、`apps/admin/src/components/JsonbObjectEditor.vue`。vite 编译 200、HMR 生效；admin types/city 是 Record<string,any>，与恢复版兼容，无需恢复。
- **教训（已入 MEMORY.md）**：**重要改动务必及时 commit/stash**，`git pull --rebase` 会静默丢弃未提交修改；找回手段 = `git stash list` + `git show stash@{n}:path`（只恢复文件、不弹栈）；git log 全历史没有的内容 = 当时未提交。
- **遗留**：stash@{2} 内还有 20+ 个文件（major/postgrad-univ/dept/guide、user 端多个页面、temp SQL 等）同样是当时未提交的工作，可能也被覆盖丢失，待用户确认是否一并恢复；建议恢复后立即 commit。

## 继续：恢复 stash@{2} 全部丢失文件（用户确认"帮我恢复"）
- 从 stash@{2} 批量恢复 30 个文件：admin(major api/types/views、university dept/guide、styles/index.css、postgrad-univ) + user(api/types/views/city、major Detail/List/PostgradList、PostgradMajorDialog、Guide) + temp 调试脚本。
- components.d.ts **不覆盖**：当前工作区版本已含恢复组件(JsonbArrayEditor/JsonbObjectEditor) + 今天新组件(ElCollapse/ElDatePicker/ElDivider)，保留现状。
- 未触碰今天的新修改：laboratory/index.vue、DepartmentTab.vue、DepartmentDetail.vue、AGENTS.md。
- 验证：admin:3000 / user:3001 全部恢复页面 vite 编译 200，HMR 生效。
- **下一步建议用户**：尽快 `git add -A && git commit`（现在 41 个文件待提交），防再次丢失。

## 检查：后端仓库 D:/exeProject/ideaProjects/Project-HaiFeng 无 git 丢失
- 结论：**后端没有 git 消失问题**。8-1 修改的全部后端文件（11 个 typeHandler mapper、MajorMapper/PostgradMajorMapper/MajorPostgradDirectionMapper/CompetitionMajorMapper、V43 migration、PostgradMajorUniversity 系列、VO）**全部已提交**在 `55081dc feat:后端`（8-2 12:57），`git diff HEAD` 为空。
- 当前工作区仅 11 个**已暂存未提交**改动（.gitignore、Need/16-20 文档、haifeng-app CityController/CityService/CityServiceImpl/WishPlanExcelUtil，PDF 分支）——风险点：尽快 commit。
- stash@{0}（WIP on PDF: 29fdae4）内容与工作区重合但更旧（CityController 少 9 行），可安全 drop。
- 对比教训：前端因未提交被 pull --rebase 覆盖大面积丢失；后端 8-2 上午已 commit（feat:后端）所以毫发无损 → **收尾必 commit**。
