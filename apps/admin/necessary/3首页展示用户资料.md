# 总的项目需求


## 接口文档
位置：apps\admin\interfaces\order3.md


## 待实现需求：
首页管理[父模块]--界面管理[子模块]
用户管理[父模块]--个人中心[子模块]

**首页内容**：无任何权限都可以访问
**用户资料表(个人中心)**：必须使用户权限才可以

1. 前端动态展示数据表里的数据，只展示信息
2. 个人中心展示个人资料



### 任务2-1【完成个人中心-用户资料展示 (t_member_profile)】
- 用户资料表(t_member_profile)

1. 接口用户资料信息，支持查和改的字段：real_name，email，gender，school_name，province，city，major，identity，grade，education_level
限制：
1.创建省份的枚举
2.专业，城市，school_name需要实现模糊查询（默认返回 10 条，最多 20 条），模糊查询用来辅助用户填写时候的选项的
3.注意：
- 仅当 identity = 大学生 或 研究生 时，才显示/允许编辑 schoolName
- 当 identity = 高中生 或 其他 时，schoolName 字段不返回/不允许修改   
- 若用户身份从 大学生/研究生 改为 高中生/其他，schoolName 自动置空
4.需要新增枚举：IdentityEnum，ProvinceEnum，GenderEnum

5. 还需要修改和展示：
1. wechat_id(AES加密存储)，avatar，phone，username，password
2. 点击查看微信号，需要解密展示具体的微信号
3. 可以修改微信号

### 任务2-2【完成个人中心-用户资料别的信息展示(t-member)】

 还需要展示下面的字段，但是不支持修改，只支持查看：
   - 邀请码
     invite_code
   - 提现金额
     commission_balance          DECIMAL(10,2)   DEFAULT 0.00,       -- 可提现余额
     commission_total_earned     DECIMAL(10,2)   DEFAULT 0.00,       -- 累计获得佣金
     commission_total_paid       DECIMAL(10,2)   DEFAULT 0.00,       -- 累计已发放佣金
   - 用户身份
     member_type

### 任务3【完成个人中心-提现功能】
1.需要字段：
- 金额
  commission_balance          DECIMAL(10,2)   DEFAULT 0.00,       -- 可提现余额
  commission_total_earned     DECIMAL(10,2)   DEFAULT 0.00,       -- 累计获得佣金
  commission_total_paid       DECIMAL(10,2)   DEFAULT 0.00,       -- 累计已发放佣金
- 邀请码
  invite_code
- 邀请人数
  联表查询邀请码，对应用户的数量
2.佣金提现
- 点击提现，先判断是否大于50或100，然后选择50或100提现（后端已将50,100写死）
- 可以的话生成一条提现记录表，将用户的id，phone，wechat_id，wechat_id_index，选择的50.00/100.00（对应amount）存储进去即可
3.支持添加用户的外键邀请码，只有一个，而且之前用户填写过邀请码的不允许修改或添加【前提：用户之前注册的时候没有填写邀请码】
逻辑：我们展示的是：referrer_id对应的invite_code。然后之前注册用户有的没有填写邀请码，这里肯定是空，那么可以让用户填写邀请码，然后联表查询返回对应的用户的username，phone，用户点击确认，把用户的referrer_id绑定成功，后续不可修改。

#### 说明 【后端都已实现】
1. 邀请码不允许填自己
2. 邀请码不允许两个人循环
3. 当用户填写的邀请码对应的用户被禁用或删除的时候，也不允许修改，我后台做了实现，你只需知道哪怕对应用户禁用或删除，也不允许修改referred_id


### 任务4【完成-界面管理】

1. 返回site_icp，contact_url（JSONB）里面所有的字段，basic_message（JSONB）里面所有的字段

## 前端渲染

### 首页
1.将返回的首页icp，公众号等信息展示和渲染到前端

####  样式*
1. 在首页的最下面设计，样式跟个人中心的样式差不多，都是以灰白为主，红橙黄颜色点缀
2. 那个微信需要展示二维码，还有微博，知乎，抖音，B站需要一个圆圈框框


### 个人中心【用户版】
1. 之前在user包实现过管理员的个人中心，现在要复用这个接口，根据不同的身份跳转不同的界面[用户的个人中心需要跳转一个新的页面]
####  样式*
查看：logos\ChatGPT Image 2026年5月13日 11_37_40.png，这是我的界面原型，需要替换的：
1. 网站名称是：海枫未来规划院
2. logos地址：logos\logo-transparent-final.png 【后续会替换成url】
3. 用户提现功能没设计原型，但是样式还是不变
4. 用户收藏和消息通知暂不设计

### 注意事项
1. 主界面没有token也可以访问，aixos可以做个判断，哪怕不登录和也可以访问
2. 个人中心需要用户权限，点击之后没登录的，需提示先登录，生成一个提示框，后续界面可以复用
3. 提示框提示信息：您还没有登录，请先登录，下面有两个按钮，取消和前往登录[跳转到登录界面，注意地址不要写死]


### 总结
主界面不限制用户，哪怕不登录也能看到，用户个人中心和资料就要提示登录了，然后跳转登录，所以你要判断他身份，只能是：'normal', 'pro', 'vip'

