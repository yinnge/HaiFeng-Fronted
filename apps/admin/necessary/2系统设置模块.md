
## 接口
apps\admin\interfaces\order2.md

## 功能实现位置
后端

## 界面设计要求
1. 之前实现过的，复用
2. 新的借鉴，使用DESIGN.md来探讨
3. 下拉菜单按钮 (推荐)                                                            
     一个“批量删除”按钮，点击后展开下拉选项：删除选中 / 删除一个月前 / 删除全部
## 功能需求

1. **系统设置模块** - 单例配置管理，包含网站基本信息、SEO配置、会员价格、社交媒体链接等
2. **用户管理模块** - 用户列表查询、详情查看、状态修改，支持微信号 AES 加密存储和盲索引查询

系统管理[父模块]--系统设置[子模块]--操作记录[子模块]
用户管理[父模块]--用户列表[子模块]

## 样式：
1. 与原样式保持一致，之前已经实现权限管理。

## 具体模块

### 系统设置
1. 获取系统设置
2. 更新系统设置

#### 说明
1.这就几个字段，平滑的展示，而不是列表


### 用户列表
1. 分页查询用户列表
2. 获取用户详情
3. 修改用户状态（禁用）
4. 查看用户微信明文
5. 会员开通/升级/续费
┌──────────┬────────────┬───────────────────────────────────────────────────┐                                                                            
  │ 查询方式 │    字段    │                       说明                        │
  ├──────────┼────────────┼───────────────────────────────────────────────────┤
  │ 模糊查询 │ phone      │ 如输入"138"可匹配"13812345678"                    │
  ├──────────┼────────────┼───────────────────────────────────────────────────┤
  │ 模糊查询 │ inviteCode │ 如输入"ABC"可匹配"ABC12345"                       │
  ├──────────┼────────────┼───────────────────────────────────────────────────┤
  │ 精准查询 │ memberType │ 必须完全匹配：normal/pro/vip                      │
  ├──────────┼────────────┼───────────────────────────────────────────────────┤
  │ 精准查询 │ status     │ 必须完全匹配：active/disabled                     │
  ├──────────┼────────────┼───────────────────────────────────────────────────┤
  │ 精准查询 │ wechatId   │ 输入微信号明文，后端先用SHA-256转盲索引再等值匹配 │
  └──────────┴────────────┴───────────────────────────────────────────────────┘
6. 用户每行旁边有几个按钮：详情，升级，禁用
7. 升级就是：会员开通/升级/续费，通过会员类型，金额，开通时间选择，自动金额，或自己输入金额，然后生效。
8. 详情里面的微信号旁边有一个按钮：查看微信号

### 操作日志
1. 分页查询操作日志
2. 获取操作日志详情
3. 批量删除操作日志

### 操作日志
1. 获取系统设置
2. 更新系统设置

## 说明
1. 接口文档已经写清楚了
2. 排版要变一下，之前的权限管理，以及下面的角色列表[子模块]--权限列表[子模块]--管理员列表[子模块]
要下移，顺序为：
1. 系统管理[父模块]
2. 首页管理[父模块]
3. 权限管理[父模块]
4. 用户管理[父模块]
额，有些模块还没有实现，先空下


